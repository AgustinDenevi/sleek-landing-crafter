import React from 'react';
import { motion } from 'framer-motion';
import HyperText from './magicui/hyper-text';

const Header = () => {
  return null; // We're not using this component anymore
};

export default Header;